const levels = {
  1 : {
    instructions : "These are some instructions for level 1!",
    codeLines : ["var action = {", "&ensp;type : <input type=text></input>", "}"],
  }
}

export default levels;
